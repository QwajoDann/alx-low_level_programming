#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print bumbers of base 10'
 * Return: Always 0
 */
int manin(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
